<h1>TicketType creation</h1>

<form action="{{ path('tickettype_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('tickettype') }}">
            Back to the list
        </a>
    </li>
</ul>
