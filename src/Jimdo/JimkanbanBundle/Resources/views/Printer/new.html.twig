<h1>Printer creation</h1>

<form action="{{ path('printer_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('printer') }}">
            Back to the list
        </a>
    </li>
</ul>
